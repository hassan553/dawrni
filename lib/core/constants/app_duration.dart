/// In this class, we will define all the specific and fixed [Duration] within the application,
/// for example, waiting times for loading or displaying the [SnackBar].
///
/// You can define [Duration] in the following way:
///
/// static const Duration durationName = Duration(seconds: secondNumber);

class AppDuration {
  static const Duration snackBarDuration = Duration(seconds: 5);
}
